// Example array containing a key-value pair
const keyValueArray = ["redirect_uri", redirectUri];

// Accessing the key and value
const key = keyValueArray[0];  // "redirect_uri"
const value = keyValueArray[1];  // The value stored in the variable redirectUri

// Using the key-value pair
console.log(key + ": " + value);
